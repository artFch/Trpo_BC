#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <stdbool.h>
void generate();
int convertNumber(int number, int numk[]);
void bulls_cows(int numk[], int guess[]);
bool take_it_or_not();
void ask_for_a_number();
#endif